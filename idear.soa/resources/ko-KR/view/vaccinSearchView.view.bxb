result-view{
  match: VaccinationInfo(result)

  message{
    
    template ("#{value(result.vaccinationName)}, #{value(result.vaccinationDesc)}") {
      // speech ("#{value(result.pregnancyStatusResult)}") // speech는 빅스비가 음성으로 해주는 말을 결정
    }
  }

}