action (VaccineCheck) {
  description (예방접종 체크)

  collect {
    input (whatMonth) {
      type (WhatMonth)
      min (Required) max (One)
    }
    input (choiseVaccine) {
      type (ChoiseVaccine)
      min (Optional) max (One)
    }
  }
  type(Search)
  output (ResultsVaccin )
}
