result-view{
  match: VaccinationInfo(result)

  message{
    
    template ("#{value(result.vaccinationName)}") {
    }

  }
  render {   
      layout-macro (VaccinSearch) {
        param (resultData){ 
          expression (result) 
        }    
  }
  }
}