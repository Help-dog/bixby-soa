result-view {
  match: PolicyResult(this)
  
	message {
		template("아래의 정책들을 확인해 보세요")
	}
  //수정한 날짜 맨위에 작게 쓰기.
  render {
		// if (size(this) == 1) {
		// 	layout-macro(BabyFoodInfo) {
		// 		param(info){
		// 			expression(this)
		// 		}
		// 	}
		// } else {
      
			list-of(this){
				where-each(item){
					layout-macro(PolicyList){
						param(policy){
							expression(item)
						}
					}
				}
			}
		// }
	}
}