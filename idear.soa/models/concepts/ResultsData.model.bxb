structure (ResultsData) {
  //가져온 데이터입니다 = [데이터]
  description (데이터를 받아왔으면 보여줄 함수)
  property (resultInData) {
    type (ResultInData)
    min (Optional) max (One)
  }
  property (operatorName) {
    type (OperatorName)
    min (Optional) max (One)
  }
}
